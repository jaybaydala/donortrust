<% content_for :html_title do %>iEnd:Poverty - <%= @user.name %><% end %>

<%= render 'iend/shared/header' %>

<% if params[:public].present? || current_user != @user %>
  <%= render 'public' %>
<% else %>
  <%= render 'private' %>
<% end %>
