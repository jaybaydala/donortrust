<%= render 'iend/shared/header' %>

<% if !logged_in?  %>
  <%= render 'public' %>
<% elsif logged_in? && (params[:public].present? || (current_user != @user && !current_user.friends_with?(@user))) %>
  <%= render 'public' %>
<% else %>
  <%= render 'private' %>
<% end %>
