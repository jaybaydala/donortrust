<%= render 'iend/shared/header' %>

<% if (params[:public].present? || current_user != @user) && !current_user.friends_with?(@user) %>
  <%= render 'public' %>
<% else %>
  <%= render 'private' %>
<% end %>
