<h1>Editing project</h1>

<%= error_messages_for :project %>

<% form_for(:projects, :url => project_path(@project), :html => { :method => :put }) do |f| %>
  <p>
    <%= render :partial => 'form' %>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', project_path(@project) %> |
<%= link_to 'Back', projects_path %>