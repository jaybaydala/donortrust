<h1>New project</h1>

<%= error_messages_for :project %>

<% form_for(:project, :url => projects_path) do |f| %>
  <p>
    <%= render :partial => 'form' %>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', projects_path %>