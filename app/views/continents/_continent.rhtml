

<div id="<%= dom_id(continent) %>"> 
  
  <%= link_to_remote  "Delete", 
                    :url => continent_path(continent),
                    :confirm => "Do you want to delete '#{continent.continent_name}'?", :method => :delete %>


<%= link_to_remote "Edit", :url => continent_path(continent) %>

  
  <%= continent.continent_name %> 
</div>
