<div class="campaign_donation line_item">
  Campaign Donation: <%= item.campaign ? link_to(item.campaign.name, iend_campaign_path(item.campaign)) : "Campaign Donation" %>
  <% if item.participant.present? %>To <%=h item.participant.user.name %><% end %>
</div>
