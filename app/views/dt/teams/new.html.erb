<h1>Create a new team</h1>

<%= error_messages_for :team %>

<% form_for([:dt, @campaign, @team], :html => {:multipart => true}) do |f| %>
	<fieldset class="campaign_form">
		<label>Team Name</label>
	    <%= f.text_field :name %>
	
		<label>Short Name</label>
		<span class="small_text">
			http://www.christmasfuture.org/dt/<%= @campaign.short_name %>/team/
		</span>
		<%= f.text_field :short_name %><%= what_is_this?'short_name', 'The short name is a small (10-20 characters) name to describe your team. It should not contain any spaces. Also this name will be used to provide your team with a Christmas Future URL. http://www.christmasfuture.org/yourCampaignShortName/team/youTeamShortName'%>
		
		<%= observe_field	:team_short_name, 
							:frequency => 1,
							:url => validate_short_name_of_new_dt_team_path(:campaign_id => @campaign),
							:with => :team_short_name
							%>
							
		<%= show_spinner("Validating ") %>
		
		<div id="errors" style="display:none;">
		</div>
		<label>Share a little about the team (this information will be made public)</label>
		<%= fckeditor_textarea( "team", "description", :toolbarSet=> 'ChristmasFuture', :height => 200 ) %> 
		
		<label>Upload a team image <50kb.  If you don't upload one a default image will be used for you.  You can also come back and upload your photo later.</label>
		<%= upload_column_field :team,  :picture  %>
		
		<label>What is your team fundraising goal?</label>
		<span class="small_text">$ </span><%= f.text_field :goal %>
		
		<label>Team Contact Email</label>
		<%= f.text_field :contact_email, :value => current_user.email %><br />
		<%= f.check_box :ok_to_contact %><span class="small_text">Allow people to contact me about this team (your email address will show on the team page)</span><br />
		<%= f.check_box :require_authorization%><span class="small_text">New team members require my approval</span>
		<p>
			<%= f.submit "Create" %>
		</p>
  	<fieldset>
<% end %>

