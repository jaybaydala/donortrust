<%-
photosource = nil
photo = Flickr::Photo.new(flickr_photo.photo_id.to_s)
begin
  photosource = photo.sizes[0]['source']
rescue
  # ignore Photo not Found error
end
-%>
<%= link_to(image_tag(photo.sizes[0]['source'], :alt => photo.to_s), "http://flickr.com/photos/#{photo.owner.id}/#{photo.id}", :rel => 'blank') if photosource -%>
