<h1>Listing programs</h1>

<table>
  <tr>
  </tr>
  
<% for program in @programs %>
  <tr>
   <td><%=h program.program_name %></td>
    <td><%=h program.contact.first_name %></td>    
    <td><%= link_to 'Show', programs_path(programs) %></td>
    <td><%= link_to 'Edit', edit_programs_path(programs) %></td>
    <td><%= link_to 'Destroy', programs_path(programs), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New programs', new_program_path %>