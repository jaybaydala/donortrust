<h1>New measure_category</h1>

<%= error_messages_for :measure_category %>

<% form_for(:measure_category, :url => measure_categories_path) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <p>
    <%= submit_tag "Create" %>
  </p>
<% end %>

<%= link_to 'Back', measure_categories_path %>